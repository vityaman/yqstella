// Ожидание: ERROR_UNEXPECTED_TYPE_FOR_EXPRESSION
language core;

fn main(n : Nat) -> Nat {
  // z должен быть Nat, но передаётся Bool
  return Nat::rec(n, false, fn (prev : Nat) {
    return fn (acc : Nat) { return acc }
  })
}

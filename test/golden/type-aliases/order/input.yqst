language core;

extend with #type-aliases;

type N = M
type O = Nat
type M = O

fn increment_twice(n : N) -> Nat {
  return succ(succ(n))
}

fn main(n : Nat) -> N {
  return increment_twice(n)
}

// Nikolai Kudasov has a bug.
fn xfail(n : Nat) -> Bool {
  return n
}

language core;

extend with
  #variants,
  #nullary-variant-labels,
  #structural-subtyping;

fn f(x : <| a, b : Bool, c : Nat |>) -> Nat {
  return 0
}

fn main(x : <| a, b : Bool |>) -> Nat {
  return f(x)
}

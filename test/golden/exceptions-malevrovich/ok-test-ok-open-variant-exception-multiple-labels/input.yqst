language core;
extend with #exceptions, #open-variant-exceptions;

exception variant Failure : Nat
exception variant Timeout : Bool
exception variant OutOfMemory : Nat

fn main(n : Nat) -> Nat {
  return try { n } with { 0 }
}

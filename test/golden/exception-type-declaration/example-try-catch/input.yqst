language core;

extend with
  #exceptions,
  #exception-type-declaration,
  #structural-patterns;

exception type = Nat

fn fail(n : Nat) -> Nat {
  return throw(succ(0))
}

fn main(n : Nat) -> Nat {
  return try {
    fail(n)
  } catch {
    0 => 0
  }
}

language core;
extend with #type-cast, #top-type, #structural-subtyping, #try-cast-as;

// success branch returns Nat, fallback returns Bool — type mismatch
fn main(n : Nat) -> Nat {
  return try { n } cast as Nat { x => x } with { false }
}
